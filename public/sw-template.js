self.addEventListener('install',event=>event.waitUntil((async()=>{const cache=await caches.open(CACHE);await cache.addAll(FILES);await self.skipWaiting();})()));
self.addEventListener('activate',event=>event.waitUntil((async()=>{const prefix=CACHE.slice(0,CACHE.lastIndexOf('-')+1);for(const key of await caches.keys())if(key.startsWith(prefix)&&key!==CACHE)await caches.delete(key);await self.clients.claim();})()));
self.addEventListener('fetch',event=>{const request=event.request,url=new URL(request.url);if(request.method!=='GET'||url.origin!==self.location.origin)return;const shell=new URL('./',self.location.href);const known=FILES.some(path=>new URL(path,shell).href===url.href);if(!known&&!(request.mode==='navigate'&&url.pathname===shell.pathname))return;event.respondWith((async()=>{const cache=await caches.open(CACHE);return await cache.match(request)||await cache.match(new URL('./index.html',shell))||fetch(request);})());});
